import {Color} from 'Display/Color'
import {Stage} from 'Display/Stage'
import {Game} from 'Demo/Game'

let stage = Stage.init(800, 600, new Color(0.3, 0.6, 0.9), 30, true)
let game: Game = new Game()
