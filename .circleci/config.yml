version: 2
jobs:
  build:
    working_directory: ~/nano-typescript
    docker:
      - image: circleci/node:4.8.2
    steps:
      - checkout
      - run:
          name: update-npm
          command: 'sudo npm install'